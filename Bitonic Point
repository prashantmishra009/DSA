	int findMaximum(int arr[], int n) {
	    // code here
	    int ans=arr[n-1];
	   for(int  i=0;i<n;i++){
	       if(arr[i]>arr[i+1]) {
	          ans=max(ans,arr[i]);
	           
	       }
	           
	   }
	   return ans;
	}
