	vector<int> kLargest(int arr[], int n, int k) {
	    // code here
	    sort(arr,arr+n);
	    vector<int>ans;
	    int j=n-1;
	    for(int i=0;i<k;i++){
	        ans.push_back(arr[j--]);
	    }
	    return ans;
	}
