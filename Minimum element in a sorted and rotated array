    int findMin(int arr[], int n){
        //complete the function here
      int mini=INT_MAX;
      for(int i=0;i<n;i++){
          if(arr[i]<mini)
          mini=arr[i];
      }
      return mini;
    }
