  int countZeroes(int arr[], int n) {
        // code here
        sort(arr,arr+n);
        int count=0;
        for(int i=0;i<n;i++){
            if(arr[i] == 1) break;
            count++;
        }
        return count;
    }
