	vector<string>find_permutation(string s)
		{
		    // Code here there
		   vector<string> result;
   sort(s.begin(), s.end());
    do {
        result.push_back(s);
    } while (next_permutation(s.begin(), s.end()));
    return result;
		}
